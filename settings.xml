<settings>
  <servers>
    <server>
      <id>nexus-local</id> <!-- This is the server ID -->
      <username>admin</username>
      <password>docker</password>
    </server>
  </servers>

  <mirrors>
    <mirror>
      <id>nexus-local</id> <!-- This is the mirror ID -->
      <url>http://192.168.0.108:31599/repository/maven-central/</url>
      <mirrorOf>central</mirrorOf>
    </mirror>
  </mirrors>
</settings>
